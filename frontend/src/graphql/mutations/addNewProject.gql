mutation AddNewProject($name: String!, $description: String!, $git: String) {
    addProject(name: $name, description: $description, git: $git) {
        id
        name
        description
        createdAt
        updatedAt
    }
}
