mutation editProject($input: EditProjectInput!) {
  editProject(input: $input) {
    project {
        id
        title
        summary
        status
        body
        publishedAt
        createdAt
        updatedAt
    }
    userErrors {
      ...MutationUserError
    }
  }
}
